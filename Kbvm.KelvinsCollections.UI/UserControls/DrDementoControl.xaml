<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Kbvm.KelvinsCollections.UI.UserControls.DrDementoControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kbvm.KelvinsCollections.UI.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls" xmlns:viewmodels="using:Kbvm.KelvinsCollections.UI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DrDementoViewModel}"
	mc:Ignorable="d">

    <Grid x:Name="ContentArea">
		<controls:ListDetailsView
			x:Uid="ShowList"
			x:Name="ShowListViewControl" 
			Background="Transparent"
			BorderBrush="Transparent"
			BackButtonBehavior="Manual"
			ItemsSource="{Binding Items}" />

		<Button x:Name="myButton">Other Button</Button>
	</Grid>
</UserControl>
